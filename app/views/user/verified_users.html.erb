<div class="ui container">
<br><br><br>

<div class="link_right">
<i class="icon cloud download"></i>
<%=
  link_to 'Export Users',
  user_verified_users_path(format: :xlsx)
%>
</div>
<div class="link_center"><strong>Total Verified Users: <%= @users.count %></strong></div>
<table class="ui compact celled definition table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Show</th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do |p| %>
    <tr>
    <% if p.first_name.present? %>
      <td><%=p.first_name%></td>
      <% else %>
      <td>-</td>
      <% end %>
      <td><%=p.email%></td>
      <td><%= link_to "Show" , user_view_user_path(id: p.id) %></td>
    </tr>
    <% end %>
  </tbody>
  <tfoot class="full-width">
    <tr>
      <th></th>
      <th colspan="4">
        <div class="ui right floated small labeled icon button">
          <i class="user icon"></i> <%= link_to "Add new user" %>
        </div>
        <div class="ui small button">
          <%= link_to "Back" , :back %>
        </div>
      </th>
    </tr>
  </tfoot>
</table>
</div>


<script>

</script>